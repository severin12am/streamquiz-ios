/**
 * Product feature toggles — flip here to re-enable without hunting through UI code.
 */

/** Apple Speech for non-MC rounds. When false, players type answers only. */
export const VOICE_ANSWERS_ENABLED = false;
